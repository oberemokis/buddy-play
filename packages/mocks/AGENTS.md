# @sync/mocks — фикстуры для тестов

Статические наборы данных (fixtures) для юнит- и интеграционных тестов.
Никакой логики — только данные.

## Структура

```
src/
  <entity>.ts        ← read-only данные для сущности
```
